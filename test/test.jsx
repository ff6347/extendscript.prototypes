#include "../dist/extendscript.prototypes.0.0.1.jsx" // jshint ignore:line#include "countries.json" // jshint ignore:line#include "babyparse.js"// jshint ignore:line// #include "json2.js"// jshint ignore:line// #include "jsonQ.js"// jshint ignore:line// var arr = [1,2,3,4,5,6];// arr.forEach(function logArrayElements(element, index, array) {//   $.writeln('a[' + index + '] = ' + element);// });var main = function () {var csv = "col1,col2,col3\n\"1\",2,3\n4,5,6"; var parsed = Baby.parse(csv); $.writeln(parsed.toSource());//   var res = data.filter(function(element){//     return element.hasOwnProperty('name') || element.hasOwnProperty('NAME');//     });// // $.writeln(res.toSource());//   // body...// // var res = data.where("( el, i, res, param ) => el[param]", "name");// res.forEach(function logArrayElements(element, index, array) {//     $.writeln(element.name['common']);//   // $.writeln('a[' + index + '] = ' + element);// });return 0;};main();